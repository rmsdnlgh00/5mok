# OpenAI API 키 (임베딩 계산용)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-...

# 임베딩 모델 (기본값: text-embedding-3-small)
EMBEDDING_MODEL=text-embedding-3-small

# 임베딩 차원 축소 (용량/속도 절약용, 생략 가능)
EMBEDDING_DIMENSIONS=512
